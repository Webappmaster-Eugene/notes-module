# Строка подключения нужна Prisma CLI даже для generate/validate — соединение при этом не открывается.
# Реальная база в задании не поднимается: тесты идут против подменённого PrismaClient.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/notes?schema=public"
